spring:
  application:
    name: gateway

server:
  port: 8763

zuul:
  routes:
    user-route:         # �����÷�ʽ�У�user-route ֻ�Ǹ�·��һ�����ƣ���������������
      service-id: user
      path: /user/**      # service-id ��Ӧ��·��


eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/